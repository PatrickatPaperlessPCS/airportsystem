<tr>
  <td>
 <!--    <%= f.collection_select :description, current_airport.inventories, :description, :description, {include_blank: "Other"}, {class: 'target'} %> -->
 <%= f.select :inventory_id, options_for_select(current_airport.inventories.map{ |c| [c.description, c.id, {'data-price'=>c.price, 'data-tax_rate'=>c.tax_rate}] }), {include_blank: "Other"}, {class: 'target'} %>

  </td>
  <td><%= f.input :long_description, label: false %></td>
  <td><%= f.input :price, label: false, input_html: { class: 'price' }%></td>
  <td><%= f.input :units, label: false %></td>
  <td><%= f.input :tax_rate, label: false, input_html: { class: 'taxtarget' }%></td>

  <td>
    <%= f.input_field :_destroy, as: :hidden %>
    <%= link_to 'Delete', '#', class: 'remove_record' %>
  </td>
</tr>



